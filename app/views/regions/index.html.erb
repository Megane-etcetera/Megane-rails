<div class="regions_base">
    <% @regions.each do |region| %>
        <%= link_to edit_region_path(region.id) do %>
            <table class="region_table">
                <tr>
                    <th class="region_th"><%= region.region_name %>地方</th>
                    <th class="region_th"><%= region.delivery_price %>円</th>
                </tr>
                <% region.prefectures.each do |prefecture| %>
                    <tr>
                        <td colspan="2"><%= prefecture.prefecture_name %></td>
                    </tr>
                <% end %>
            </table>
        <% end %>
    <% end %>
</div>